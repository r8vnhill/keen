/*
 * Copyright (c) 2023, Ignacio Slater M.
 * 2-Clause BSD License.
 */


package cl.ravenhill.keen.operators.alteration.mutation

import io.kotest.core.spec.style.FreeSpec

class MutatorTest : FreeSpec({

    "A Mutator instance" - {
        "when mutating an individual" - {
            "should mutate all chromosomes if the mutation rate is 1" {
//                checkAll(Arb.baseMutator(chromosomeRate = Arb.constant(1.0))) { mutator ->
//                    val individual = Arb.individual(Arb.int(0..100)).next()
//                    val mutated = mutator.mutate(individual)
//                    mutated.chromosomes.forEach { it shouldBe mutator.mutateChromosome(it) }
//                }
            }
        }
    }
})
