//[keen-core](../../../index.md)/[cl.ravenhill.keen.evolution.executors.construction](../index.md)/[CoroutineConcurrentConstructor](index.md)/[CoroutineConcurrentConstructor](-coroutine-concurrent-constructor.md)

# CoroutineConcurrentConstructor

[common]\
constructor(scope: CoroutineScope = CoroutineScope(Dispatchers.Default))

Initializes a new instance of `CoroutineConcurrentConstructor` with the specified `CoroutineScope`.

#### Parameters

common

| | |
|---|---|
| scope | The `CoroutineScope` in which the concurrent operations are executed. Defaults to `CoroutineScope(Dispatchers.Default)`. |
