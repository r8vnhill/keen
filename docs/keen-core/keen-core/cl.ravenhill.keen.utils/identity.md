//[keen-core](../../index.md)/[cl.ravenhill.keen.utils](index.md)/[identity](identity.md)

# identity

[common]\
fun &lt;[T](identity.md)&gt; [identity](identity.md)(t: [T](identity.md)): [T](identity.md)

Returns the input value unchanged.

The `identity` function is a simple utility that returns its input parameter `t` without modification. This function is commonly used in functional programming as a default or no-op function, where an operation needs to be provided but no transformation or processing of the input is required.

#### Return

The input value `t` unchanged.

#### Parameters

common

| | |
|---|---|
| T | The type of the input value. |
| t | The input value to be returned. |
